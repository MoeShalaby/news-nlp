import { NPLText, NPLUrl } from '../../src/client/js/NPL';

describe('Testing NPLText funciton', () => {
  it('Should be defined', () => {
      expect(NPLText).toBeDefined();
  });
});

describe('Testing NPLUrl funciton', () => {
  it('Should be defined', () => {
      expect(NPLUrl).toBeDefined();
  });
});
